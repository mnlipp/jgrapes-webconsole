if (project.name != "org.jgrapes.portal.base") {
    task javadocJar(type: Jar) {
        from ("${project.rootDir}/misc/javadoc-redirect")
        classifier "javadoc"
    }
    
    artifacts {
        archives javadocJar
    }
}
