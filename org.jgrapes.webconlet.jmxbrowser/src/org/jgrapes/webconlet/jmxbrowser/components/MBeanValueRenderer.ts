import { defineComponent, getCurrentInstance, onUnmounted } from 'vue';

export default defineComponent({
    props: {
        value: { required: true }
    },
    
    setup(props) {
        return {  };
    }
});
